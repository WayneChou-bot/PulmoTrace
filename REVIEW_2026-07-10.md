# PulmoTrace / CXR-Seg — 審閱報告

**日期**:2026-07-10  ·  **範圍**:程式碼正確性、Demo 部署、可優化項目、新功能建議、介面重新設計、來源引用
**定位提醒**:研究 / 教學用途,**非診斷、非醫療器材**。以下建議皆以此定位為前提。

---

## 摘要(先看這段)

整體品質相當高:有外部驗證(JSRT gold)、patient-level 切分、bootstrap CI、失敗分析、CLAIM 2024 自評與 model card——這正是多數醫療影像 AI 缺少的「誠實佐證」。程式面 27 個 pytest 全數通過,ONNX 輸出通道(3 通道:左肺/右肺/心)與網頁前端的索引解析一致,推論管線正確。

我**沒有發現會導致結果錯誤的嚴重 bug**,但找到 1 個部署層級的實際問題、數個一致性/穩健性風險,以及明確的優化空間。介面已重新設計並就地更新(見最後一節)。

---

## 1. 錯誤與風險

### 1.1 需要修正
- **部署版的 GitHub 連結指向裸網域**。抓取線上版 `https://pulmo-trace.vercel.app/` 時,頁首與頁尾的「GitHub / 原始碼」連結是 `https://github.com/`(缺 repo 路徑)。你的本機 `web/index.html` 已修正為 `.../WayneChou-bot/PulmoTrace`(commit「fix GitHub links」),但**線上版似乎尚未重新部署**。→ 請重新 deploy 一次 Vercel,並實際點連結確認。

### 1.2 一致性風險(不會報錯,但會讓「線上結果」與「論文數字」不完全對得上)
- **前處理的縮放方式與訓練/評估管線不同**。Python 端 `preprocess()` 用 `xrv.datasets.XRayResizer`,它是把影像**拉伸成正方形**(不保留長寬比);網頁端 `loadImageToBase()` 則是**保留長寬比、黑邊填滿(letterbox)**。對非正方形胸片,兩者送進模型的畫面不同,分割結果會有落差。報告中的 0.856 / 0.918 是用「拉伸」管線量出來的,所以瀏覽器上的結果嚴格說不等於報告數字。
  - 建議:二選一並在 model card 註明。若要與報告數字一致 → 網頁改成拉伸;若偏好解剖比例正確的顯示 → 維持 letterbox,但在頁面標註「瀏覽器前處理與評估管線略有差異」。(我在改版中保留 letterbox,未偷改,交由你決定。)

### 1.3 穩健性 / 邊界情況
- **`res.prob || res[Object.keys(res)[0]]`**:輸出節點名寫死為 `prob`,有 fallback 尚可,但換模型時容易踩雷,值得加一行註解。
- **多執行緒實際上沒有生效**:`ort.env.wasm.numThreads` 依賴 `crossOriginIsolated`,但 `vercel.json` 沒有設 `COOP/COEP` 標頭,所以永遠是單執行緒(功能正常、只是較慢)。要開多執行緒需加:
  ```json
  { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
  { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
  ```
  注意:開了之後所有跨源資源(含 HF 模型、CDN)都要帶正確 CORS 標頭,需一併測試。
- **`.gitignore` 註記「int8 won't run in onnxruntime-web」**:這句偏保守。ORT Web 的 wasm backend 多數情況可跑 int8(QDQ/QOperator)量化模型。若成立,把預設模型從 fp32(263 MB)換成 int8(64 MB)或 fp16(126 MB)可大幅改善首次載入。值得實測一次再下定論。

---

## 2. 可優化項目(已在改版中實作的標 ✅)

- ✅ **模型快取(Cache Storage API)**:263 MB 的 fp32 模型原本每次進站都可能重抓。改版加入 `caches` 持久化,第二次造訪近乎即時載入(這是目前最有感的體驗優化)。
- **改用更小的模型**:fp16(126 MB)或 int8(64 MB)。需先在 ORT Web 實測 int8 是否可跑;可保留 fp32 作為 fallback。
- **windowing(亮度/對比)**:放射閱片最常用的互動,原本沒有。✅ 已加入亮度、對比滑桿。
- **zoom / pan**:✅ 已加入放大縮小與拖曳平移。
- **不確定性視覺化**:分割最好呈現「模型不確定的邊界帶」。✅ 已加入「不確定帶」(機率 0.35–0.65 的像素以琥珀色標示),降低 automation bias。
- **CTR 量測線**:把心寬/胸寬那兩條最寬列直接畫在影像上,讓 0.xx 的數字「看得到怎麼算的」。✅ 已加入。
- **信心以量條呈現**:✅ 肺/心信心改成標籤+量條而非純文字。

---

## 3. 讓 Demo「更強」的功能建議(依投報比排序)

1. **DICOM 上傳支援**:很多人手上的是 `.dcm`。用 `dicomParser`(或 cornerstone 的 wado 工具)在瀏覽器解析,能大幅提升「真的可用」的感覺。仍全程本機、不上傳。
2. **量化的 CTR 不確定區間**:對遮罩邊界做小幅擾動(morphological ±k px)重算 CTR,輸出一個範圍而非單點——這比單一數字誠實得多,也和專案「honest evidence」的主張一致。
3. **一鍵「模型不會什麼」示例集**:放幾張已知會失敗的難片(portable/AP、大量積液),讓使用者主動看到極限——反而增加信任。
4. **可分享的結果卡**:把疊圖 + CTR + 免責聲明輸出成一張 PNG/連結(目前只有下載遮罩),適合教學與 LinkedIn。
5. **鍵盤快捷鍵**:`空白鍵` 切換疊圖顯示/隱藏(閱片者習慣快速比對原圖與遮罩)。
6. **模型/資料溯源面板**:每個數字都能點到來源(見第 5 節,已把引用列入頁面)。

> 醫療領域補充:上述功能都要維持「非診斷」框架。任何「偵測疾病 / 判讀是否異常」的功能都會讓它從教學 demo 變成潛在醫療器材,不建議加入。

---

## 4. 介面重新設計(已完成、就地更新 `web/index.html`)

原版樸素、偏「報告網頁」。改版參考真實放射 AI 產品(Lunit INSIGHT CXR、Qure.ai qXR、Annalise.ai)與開源閱片器(OHIF Viewer、3D Slicer、MONAI Label)的慣例,朝「影像工作站(workstation)」風格重做:

- **深色影像畫布 + 單一 teal/cyan 強調色**(OHIF 慣例):影像維持純灰階,UI 狀態與 AI 疊圖用不同顏色區分。
- **左側工具列**(反相、CTR 量測、放大、縮小、重設)+ 大畫布 + 右側結果面板 的三段式版面。
- **色盲友善疊圖**:肺 = cyan、心 = rose,附圖例(legend)。
- **CTR 儀表**改為漸層量表 + 0.50 PA 參考標記;**信心**改為量條。
- **首次造訪的引導/免責 modal** + 頁首/頁尾常駐「非臨床使用」徽章(醫療 AI 介面的必要元素,不是小字免責)。
- **新增「來源與引用」區塊**:把第 5 節所有引用直接放進頁面(CheXmask CC BY 4.0 依授權本就需要標註)。
- 保留原有 zh/en 雙語、Web Worker 推論、影像不上傳等一切既有行為。**推論核心(模型 URL、正規化、通道解析、閾值)未更動**,已通過靜態驗證。

> 原始檔已備份為 `web/index.original.backup.html`,可隨時還原。

---

## 5. 來源與引用(醫療數字都應可溯源)

1. **TorchXRayVision** — Cohen JP, et al. *A library of chest X-ray datasets and models.* MIDL 2022. arXiv:2111.00595 · https://github.com/mlmed/torchxrayvision
2. **ChestX-Det + PSPNet 分割模型** — Lian J, et al. *A Structure-Aware Relation Network for Thoracic Diseases.* IEEE TMI 2021. arXiv:2104.10326 · https://github.com/Deepwise-AILab/ChestX-Det-Dataset
3. **CheXmask v0.4(內部 silver 參考)** — Gaggion N, et al. *CheXmask: a large-scale dataset of anatomical segmentation masks.* Scientific Data 2024;11:511. **CC BY 4.0.** https://www.nature.com/articles/s41597-024-03358-1 · https://physionet.org/content/chexmask-cxr-segmentation-data/1.0.0/
4. **JSRT 資料庫** — Shiraishi J, et al. *Development of a digital image database for chest radiographs.* AJR 2000;174:71–74. https://www.ajronline.org/doi/10.2214/ajr.174.1.1740071 · http://db.jsrt.or.jp/eng.php
5. **SCR gold 遮罩** — van Ginneken B, et al. *Segmentation of anatomical structures in chest radiographs.* Medical Image Analysis 2006;10:19–40. https://zenodo.org/records/7056076
6. **NIH ChestX-ray14** — Wang X, et al. *ChestX-ray8.* CVPR 2017. arXiv:1705.02315
7. **心胸比參考範圍**(PA 正常約 0.42–0.50,>0.50 通常視為異常;不應在 AP 片量測)— Radiopaedia: https://radiopaedia.org/articles/cardiothoracic-ratio · Truszkiewicz K, et al. 2021 (PMC8125954): https://pmc.ncbi.nlm.nih.gov/articles/PMC8125954/
8. **CLAIM 2024 檢核表** — Tejani AS, et al. *Checklist for Artificial Intelligence in Medical Imaging (CLAIM): 2024 Update.* Radiology: AI 2024. https://pubs.rsna.org/doi/full/10.1148/ryai.240300

---

## 建議的下一步

1. 重新部署 Vercel,並確認 GitHub 連結正確(§1.1)。
2. 決定前處理縮放策略,並在 model card 註明(§1.2)。
3. 實測 int8/fp16 是否可在 ORT Web 執行;可跑就換小模型當預設(§1.3 / §2)。
4. 若要多執行緒,替 `vercel.json` 加 COOP/COEP 並回歸測試(§1.3)。

*本報告為技術審閱,非醫療建議。此專案不得用於任何臨床決策。*

---

## 變更記錄(2026-07-10 實作)

以下項目已實作並就地更新於 `web/index.html`(原版備份:`web/index.original.backup.html`)。所有變更皆通過靜態驗證(JS 語法、元素 ID、i18n 覆蓋、結構平衡)與生物標記數學的合成資料測試。

**模型與前處理**
- 模型改為**優先載入 fp16(131 MB)**,抓不到時自動 fallback 至 fp32;fp16 已於 wasm runtime 實測與 fp32 數值等價(無 NaN)。快取版本升至 `cxrseg-model-v2`。
  - fp16 位址:`https://huggingface.co/ShihHua/cxr-seg-pspnet/resolve/main/pspnet_lunghearts_fp16.onnx`(已上傳)。
- **前處理改為拉伸至 512²**(`drawImage(img,0,0,512,512)`),與評估管線的 `xrv XRayResizer` 一致,使瀏覽器的 CTR/遮罩對應到報告的 Dice 數字。
- 模型快取(Cache Storage API):首次下載後,再次造訪近乎即時。

**適足性 QC + 幾何生物標記面板**
- 分割後即時計算並顯示:面積 CTR(心/胸面積)、左右肺對稱 %、肺野縱向覆蓋 %、縱膈置中偏移 %。
- 「拍攝適足性」以幾何代理指標亮出「通過 / 建議檢視」徽章(左右對稱/旋轉、吸氣覆蓋、縱膈置中),直接回答「這張 CTR 能不能信」。全部標明非診斷。
- 為計算對稱性,模型的左肺/右肺通道改為分開保留(原為合併)。

**縱向前後片比較(interval change)**
- 新增「縱向比較」區塊:載入舊片(Prior)與新片(Current),各跑一次分割,輸出 CTR / 心臟面積 / 肺野面積的 Δ 變化(帶方向箭頭與顏色),並在兩張小圖畫出肺/心輪廓供並排比對。
- 復用既有模型與分割邏輯,模型僅載入一次。強調兩張須為可比較的同體位(PA)影像。

**介面**
- 深色影像工作站風格、左側工具列、色盲友善疊圖與圖例、CTR 儀表與信心量條、首訪引導/免責 modal、常駐非臨床徽章、新增「來源與引用」區塊(8 項含連結)。
- 亮度/對比 windowing、放大/平移、CTR 量測線、機率不確定帶。

**待辦**
- 重新部署 Vercel(順帶修正線上版 GitHub 連結)。
- (可選)`.gitignore` 內「int8 won't run in onnxruntime-web」的註記可更新為:已改用 fp16 作為預設。
